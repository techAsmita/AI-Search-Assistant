import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF0F0F0F);
  static const sideNav = Color(0xFF1A1A1A);
  static const card = Color(0xFF1E1E1E);
  static const searchBar = Color(0xFF2A2A2A);
  static const searchBarBorder = Color(0xFF3A3A3A);
  static const footer = Color(0xFF141414);

  // Text & Icons
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFB0B0B0);
  static const textMuted = Color(0xFF777777);
  static const iconGrey = Color(0xFF9E9E9E);

  // Accent
  static const accent = Color(0xFF20B2AA); // soft teal accent
  static const accentSoft = Color(0x3320B2AA);
}
